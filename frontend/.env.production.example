# Deployed FastAPI base URL (HTTPS, no trailing slash, no /api suffix).
# Test in browser: https://YOUR-URL/api/health  →  {"status":"ok",...}
EXPO_PUBLIC_BACKEND_URL=https://your-service-xxxxx.run.app
